# Copiar este archivo a .env y completar los valores antes de levantar el stack.
# Generar passwords fuertes en produccion, por ejemplo con:
#   openssl rand -hex 16

DB_HOST=db
DB_NAME=mislupins
DB_USER=mislupins
DB_PASS=changeme
DB_ROOT_PASS=changeme